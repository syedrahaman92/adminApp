import {Platform} from "react-native"
// import * as WebBrowser from "expo-web-browser"

let defaultBrowser: string | undefined

// export async function getDefaultBrowser() {
//   if (Platform.OS === "android" && !defaultBrowser) {
//     const tabsSupportingBrowsers = await WebBrowser.getCustomTabsSupportingBrowsersAsync()
//     defaultBrowser = tabsSupportingBrowsers?.defaultBrowserPackage
//   }

//   return defaultBrowser
// }
